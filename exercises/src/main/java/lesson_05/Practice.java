package lesson_05;

// This is the class definition. It must match the filename (Practice.java).
public class Practice {

    // The main method is the starting point of a Java application.
    public static void main(String[] args) {

        // =============================
        // EXAMPLE: IF-ELSE STATEMENT
        // =============================

        // int attendees = 4;  // Number of attendees at an event
        // boolean largeVenue; // Will be true if we need a large venue

        // if (attendees >= 5) {
        //     largeVenue = true;
        //     System.out.println("We are going to need a large venue");
        // } else {
        //     largeVenue = false;
        //     System.out.println("No need for a large venue");
        // }

        // =============================
        // WORKING WITH ARRAYS
        // =============================

        // Declaring individual variables (not efficient)
        // String itemDesc1 = "Shirt";
        // String itemDesc2 = "Trousers";
        // String itemDesc3 = "Scarf";

        // Using an array to hold multiple values
        // String[] items = {"Shirt", "Trousers", "Scarf"};

        // Array of integers
        // int[] numbers = {27, 12, 82, 70, 54, 1, 30, 34};

        // String array
        // String[] names = {"Mary", "Bob", "Carlos"};

        // Assigning values to an existing array
        // String[] names = new String[3];
        // names[0] = "Mary";
        // names[1] = "Bob";
        // names[2] = "Carlos";

        // Array of ages
        // int[] ages = {25, 27, 48};

        // Another way to initialize an array
        // int[] ages2 = new int[3];
        // ages2[0] = 25;
        // ages2[1] = 27;
        // ages2[2] = 48;

        // Example of accessing array elements
        // System.out.println(names[0] + " is " + ages[0] + " years old ");

        // =============================
        // PROCESSING ARRAY ELEMENTS
        // =============================

        // Loop through an array of names
//        String[] names = {"George", "Jill", "Xinyi", "Ravi"};
//        for (String n : names) {
//            System.out.println("Name is " + n);
//        }
//        System.out.println();

        // =============================
        // CHECK IF ANYONE PASSED
        // =============================
//        int passmark = 12; // Passing score
//        boolean passed = false; // To track if someone passed
//        int[] scores = {4, 6, 2, 8, 12, 35, 9}; // List of scores
//
//        for (int s: scores) {
//            if (s >= passmark) {
//                passed = true;
//                break; // Exit the loop as soon as one passing score is found
//            }
//        }
//        System.out.println("At least one learner passed? " + passed);
    }
}

/*
Summary:
--------
This Java class demonstrates various basic concepts:
1. How to use conditional statements (`if-else`) to make decisions.
2. How to declare and use arrays to store multiple values.
3. Using loops (especially `for-each`) to process array elements.
4. Using a `break` statement to exit a loop early when a condition is met.

These examples are useful for understanding how Java handles basic data processing.
*/
